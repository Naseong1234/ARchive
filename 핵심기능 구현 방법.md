# 핵심기능 구현 방법

## 문서 목적
- 이 문서는 현재 `ARchive_AI` 프로젝트에서 구현한 핵심 기능 4가지를 초보자도 이해할 수 있게 설명하기 위한 문서입니다.
- 단순히 "무엇을 만들었는지"만 정리하는 것이 아니라, "왜 이렇게 만들었는지"와 "코드에서 어떤 순서로 동작하는지"를 함께 설명합니다.

---

## 1. 모바일 사진을 앱으로 연결하는 기능

### 어떤 기능인가요
- 사용자가 휴대폰 갤러리에서 사진 1장을 선택하면, 그 사진 경로를 앱이 전달받는 기능입니다.
- 이 기능은 로그인 씬에서 시작되며, 이후 퍼즐 생성과 AR 마커용 이미지 분할의 출발점이 됩니다.

### 왜 필요한가요
- 이 프로젝트는 사용자의 실제 추억 사진을 퍼즐과 AR 콘텐츠에 활용하는 구조입니다.
- 따라서 앱 내부에 미리 준비된 고정 이미지가 아니라, 사용자가 직접 선택한 이미지를 런타임 중에 받아와야 합니다.

### 어떤 스크립트가 담당하나요
- `ARchive_AI/Assets/Data/Scripts/Image/sh_ImagePickerService.cs`
- `ARchive_AI/Assets/Data/Scripts/Scene/sh_LoginSceneController.cs`

### 구현 방법
1. 로그인 씬에서 사용자가 파일 첨부 버튼을 누르면 `sh_LoginSceneController.OpenImagePickerFromButton()`이 호출됩니다.
2. 이 함수는 `sh_ImagePickerService.PickImage(...)`를 호출해서 이미지 선택 작업을 시작합니다.
3. 에디터에서는 `EditorUtility.OpenFilePanelWithFilters(...)`를 사용해서 테스트용 파일 선택 창을 엽니다.
4. 안드로이드 기기에서는 `NativeGallery` 플러그인을 사용해서 갤러리 접근 권한을 확인하고, 권한이 있으면 갤러리에서 이미지를 선택합니다.
5. 사진 선택이 성공하면 선택된 파일 경로를 `HandleImagePickSuccess(string selectedPath)`로 전달합니다.
6. 선택이 취소되거나 권한이 거부되면 각각 취소 메시지나 실패 메시지를 UI에 보여줍니다.

### 코드에서 중요한 부분
- 한 번 선택 중일 때 다시 선택 요청이 들어오지 않도록 `IsPicking` 상태값으로 중복 실행을 막았습니다.
- 성공, 취소, 실패를 각각 콜백으로 분리해서 이후 흐름을 안전하게 연결했습니다.
- 에디터와 안드로이드 처리를 분리해서, 개발 중 테스트와 실제 기기 동작을 모두 지원하도록 만들었습니다.

### 사용자가 확인할 점
- 안드로이드에서 갤러리 접근 권한이 허용되어 있어야 합니다.
- 로그인 씬의 버튼 `On Click()`이 `sh_LoginSceneController.OpenImagePickerFromButton()`에 연결되어 있어야 합니다.
- `sh_ImagePickerService`가 같은 오브젝트 또는 참조 가능한 위치에 붙어 있어야 합니다.

---

## 2. 받은 사진을 잘라서 온라인 폴더처럼 사용하는 런타임 저장 폴더에 저장 후 꺼내쓰는 기능

### 어떤 기능인가요
- 사용자가 선택한 사진을 먼저 작업용 원본 파일로 저장하고,
- 그 원본 파일을 다시 3x3으로 잘라서 퍼즐 조각과 AR 마커 조각으로 만듭니다.
- 잘라진 파일들은 런타임 중 다시 읽어서 퍼즐 UI와 AR 이미지 추적에 사용됩니다.

### 왜 에셋 폴더에 로컬로 넣지 않았나요
- `Assets` 폴더는 개발자가 에디터에서 관리하는 프로젝트 자산 폴더입니다.
- 사용자가 앱 실행 중 고른 사진은 빌드 이후에 새로 생기는 데이터이기 때문에, 일반적인 `Assets` 리소스처럼 미리 포함해둘 수 없습니다.
- 모바일 앱에서는 런타임에 생성한 파일을 `Application.persistentDataPath` 아래에 저장하는 방식이 더 적합합니다.
- 이 경로는 앱이 실행 중 새 파일을 만들고, 다시 읽고, 앱을 껐다 켜도 유지할 수 있는 실제 기기 저장 영역입니다.
- 그래서 문서상으로는 "온라인 폴더처럼 꺼내 쓰는 저장소"처럼 느껴질 수 있지만, 실제 구현은 외부 서버가 아니라 기기 내부의 영구 저장 폴더를 사용한 구조입니다.

### 어떤 스크립트가 담당하나요
- `ARchive_AI/Assets/Data/Scripts/Image/sh_ImageStorageService.cs`
- `ARchive_AI/Assets/Data/Scripts/Image/sh_ImageSliceService.cs`
- `ARchive_AI/Assets/Data/Scripts/Scene/sh_LoginSceneController.cs`

### 구현 순서
1. `sh_LoginSceneController`가 선택된 이미지 경로를 받습니다.
2. `sh_ImageStorageService.SaveImage(sourceFilePath)`를 호출해서 원본 이미지를 `Application.persistentDataPath/Data/Image/SourceImage` 아래에 복사 저장합니다.
3. 저장이 끝나면 저장된 새 파일 경로를 `SavedImagePath`로 기억합니다.
4. 그 다음 `sh_ImageSliceService.SliceSavedImage(SavedImagePath)`를 호출합니다.
5. 이 함수는 저장된 원본 파일을 `Texture2D`로 불러옵니다.
6. 가로 3칸, 세로 3칸 기준으로 이미지를 9조각으로 나눕니다.
7. 1차 분할 결과는 `MarkerImage` 폴더에 저장합니다.
8. 1차 분할 결과 중 마커로 쓰기 적합한 조각 1개를 선택합니다.
9. 선택된 1차 조각을 다시 3x3으로 잘라서 2차 회전 퍼즐용 조각들을 `RotatingImage` 폴더에 저장합니다.
10. 저장된 파일 경로들과 선택된 마커 조각 정보는 이후 퍼즐 씬과 AR 씬에서 다시 불러와 사용합니다.

### 왜 이런 과정을 거치나요
- 퍼즐 게임은 이미지 조각이 필요하고, AR 기능은 추적 성능이 좋은 마커 이미지가 필요합니다.
- 원본 1장을 바로 AR 마커로 쓰는 것보다, 특징점이 더 뚜렷한 조각 1개를 선택하는 편이 인식 안정성에 더 유리합니다.
- 또한 퍼즐 조각은 런타임 중 생성되므로, 파일 경로 기준으로 저장해두면 씬이 바뀌어도 다시 읽어 사용할 수 있습니다.

### 마커로 쓰기 적합한 조각을 찾는 기능은 어떻게 구현했나요
- 이 기능은 `sh_ImageSliceService` 안의 `SelectMarkerPieceIndex(...)`에서 처리합니다.
- 단순 완전 랜덤이 아니라, 먼저 각 1차 분할 조각의 "마커 후보 점수"를 계산한 뒤 그중 좋은 후보군 안에서 랜덤 선택하는 방식입니다.

### 동작 순서
1. 1차 분할로 만들어진 9개 조각 경로를 하나씩 확인합니다.
2. 각 조각 파일을 다시 `Texture2D`로 불러옵니다.
3. `EvaluateMarkerCandidateScore(...)`를 사용해서 각 조각의 점수를 계산합니다.
4. 계산된 점수는 `(조각 번호, 점수)` 형태로 목록에 저장합니다.
5. 모든 후보 점수를 높은 순서대로 정렬합니다.
6. 상위 후보 중에서 `markerCandidateScoreThreshold` 이상인 조각만 따로 추립니다.
7. 이 유효 후보가 1개 이상 있으면, 그 후보들 안에서 랜덤으로 1개를 선택합니다.
8. 조건을 만족하는 후보가 하나도 없으면, 가장 점수가 높은 조각 1개를 선택합니다.
9. 만약 점수 계산 자체가 불가능한 예외 상황이면, 마지막 안전장치로 전체 조각 중 완전 랜덤 1개를 선택합니다.

### 점수는 무엇을 기준으로 계산하나요
- 현재 구현은 "밝기 분산"과 "강한 경계선 밀도"를 기준으로 점수를 계산합니다.
- 쉽게 말하면, 너무 단색이거나 특징이 약한 조각보다 명암 차이가 있고 경계가 뚜렷한 조각에 더 높은 점수를 주는 방식입니다.

### 밝기 분산 계산 방식
- 각 픽셀의 밝기를 구합니다.
- 밝기는 `R, G, B` 값을 이용해서 사람이 느끼는 밝기와 비슷하게 계산합니다.
- 전체 픽셀 밝기의 평균과 분산을 구해서, 이미지 안에 밝기 변화가 얼마나 다양한지 확인합니다.
- 밝기 변화가 클수록 단조로운 이미지보다 마커 후보로 더 유리하다고 판단합니다.

### 경계선 밀도 계산 방식
- 각 픽셀에 대해 오른쪽 픽셀, 아래쪽 픽셀과 밝기 차이를 비교합니다.
- 이 차이가 일정 기준보다 크면 "강한 경계"가 있다고 판단합니다.
- 전체 픽셀 중 이런 강한 경계가 얼마나 많이 존재하는지 비율을 계산합니다.
- 윤곽과 패턴이 뚜렷한 조각일수록 이 값이 높아집니다.

### 최종 점수 계산 방식
- 정규화한 밝기 분산 값에 60%
- 정규화한 경계 밀도 값에 40%
- 이 두 값을 합쳐 최종 점수를 만듭니다.

### 왜 이런 방식으로 만들었나요
- AR 마커로 좋은 이미지는 보통 명암 차이가 있고, 패턴이나 경계가 분명한 경우가 많습니다.
- 반대로 하늘, 벽, 흐린 배경처럼 단조로운 조각은 추적 특징점이 부족해서 인식 안정성이 떨어질 수 있습니다.
- 그래서 이 프로젝트에서는 조각마다 간단한 시각적 특징 점수를 계산해서, 완전 랜덤보다 더 안정적인 후보를 고르도록 구성했습니다.

### 코드에서 중요한 부분
- `Directory.CreateDirectory(...)`로 저장 폴더가 없으면 먼저 생성합니다.
- `File.Copy(...)`, `File.WriteAllBytes(...)`를 사용해 실제 이미지 파일을 저장합니다.
- `PlayerPrefs`에는 마지막 원본 경로와 선택된 마커 조각 경로를 기록해서 다른 씬에서도 다시 찾을 수 있게 했습니다.
- `Application.persistentDataPath`를 사용했기 때문에 앱 종료 후에도 파일이 유지됩니다.
- 마커 조각 선택은 `SelectMarkerPieceIndex(...)`, `EvaluateMarkerCandidateScore(...)`, `GetPixelBrightness(...)` 함수가 핵심입니다.

### 사용자가 확인할 점
- 저장 서비스와 분할 서비스가 `sh_LoginSceneController`에 참조 연결되어 있어야 합니다.
- 퍼즐 씬과 AR 씬에서 경로를 읽어 사용할 관련 스크립트가 연결되어 있어야 합니다.
- 테스트 후 실제 기기 저장 폴더에 이미지 파일이 생성되는지 확인하면 흐름 점검에 도움이 됩니다.

---

## 3. Main씬에서 화면 드래그를 통해 퍼즐 조각을 움직이게 한 기능

### 어떤 기능인가요
- 사용자가 퍼즐 조각을 손가락이나 마우스로 누르고 끌어서 원하는 위치로 이동시키는 기능입니다.
- 짧게 터치만 하면 회전 처리로 넘기고, 실제로 끌었을 때만 드래그로 처리하도록 분리했습니다.

### 어떤 스크립트가 담당하나요
- `ARchive_AI/Assets/Data/Scripts/Puzzle/sh_PuzzleDragHandler.cs`
- 연결 대상 컨트롤러: `sh_PuzzleBoardController`

### 구현 방법
1. 각 퍼즐 조각 UI 오브젝트에 `sh_PuzzleDragHandler`를 붙입니다.
2. 이 스크립트는 `IPointerDownHandler`, `IBeginDragHandler`, `IDragHandler`, `IEndDragHandler`, `IPointerUpHandler` 인터페이스를 구현합니다.
3. 사용자가 조각을 누르면 `OnPointerDown`에서 처음 터치 위치를 저장합니다.
4. 드래그가 시작되면 `OnBeginDrag`에서 현재 슬롯 정보와 원래 위치를 기억합니다.
5. 드래그 중에는 `RectTransformUtility.ScreenPointToWorldPointInRectangle(...)`를 사용해서 현재 포인터 위치를 UI 좌표로 바꿉니다.
6. 변환된 좌표에 조각과 포인터 사이의 오프셋을 더해서, 손가락 아래에서 이미지가 갑자기 튀지 않도록 자연스럽게 이동시킵니다.
7. 드래그가 끝나면 `sh_PuzzleBoardController.HandlePieceEndDrag(this)`로 넘겨서 정답 슬롯 배치 여부를 판정합니다.
8. 드래그가 아니라 짧은 탭이었다면 `HandlePieceTapped(this)`를 호출해서 회전 기능으로 연결합니다.

### 왜 이렇게 구현했나요
- UI 퍼즐은 월드 좌표보다 `RectTransform` 기반 이동이 더 안정적입니다.
- 드래그와 탭을 분리하지 않으면, 회전하려고 눌렀을 때 조각이 움직이거나 드래그하려고 했는데 회전되는 문제가 생길 수 있습니다.
- 그래서 `tapMovementThreshold` 값을 두고, 일정 거리 이상 움직였을 때만 드래그로 판단했습니다.
- 또 `CanvasGroup.blocksRaycasts`를 조절해서 드래그 중에는 슬롯 판정이 정상적으로 들어가도록 했습니다.

### 코드에서 중요한 부분
- `pointerToPieceOffset`으로 드래그 시작 순간의 상대 위치를 저장합니다.
- `returnWorldPosition`으로 원래 돌아갈 위치를 기억합니다.
- `previousSlotBeforeDrag`, `previousSpawnPointBeforeDrag`를 기록해서 배치 실패 시 원위치 복귀가 가능합니다.
- `SetLocked(true)` 상태에서는 더 이상 드래그되지 않도록 막았습니다.

### 사용자가 확인할 점
- 퍼즐 조각 프리팹에 `sh_PuzzleDragHandler`, `CanvasGroup`, `sh_PuzzlePieceUI`가 정상 연결되어 있어야 합니다.
- 퍼즐 보드 컨트롤러가 드래그 시작, 종료, 탭 이벤트를 받아서 처리할 수 있어야 합니다.
- 퍼즐 조각이 들어갈 슬롯 오브젝트와 스폰 위치 오브젝트 구성이 먼저 준비되어 있어야 합니다.

---

## 4. 원본 이미지를 기기에 자체 저장하는 기능

### 어떤 기능인가요
- 퍼즐과 AR 작업용 원본 이미지는 세션용 저장소에 따로 보관하고,
- 추가로 로그인 씬에서 다시 보여주기 위한 원본 이미지는 기기 내부 보관용 폴더에 별도로 저장하는 기능입니다.
- 이 보관 이미지는 앱을 종료했다가 다시 실행해도 유지됩니다.

### 어떤 스크립트가 담당하나요
- `ARchive_AI/Assets/Data/Scripts/Scene/sh_LoginSceneController.cs`
- 일부 연동: `ARchive_AI/Assets/Data/Scripts/Image/sh_ImageStorageService.cs`

### 구현 방법
1. 사용자가 사진을 선택하면 먼저 `sh_ImageStorageService`가 작업용 원본 파일을 저장합니다.
2. 그 다음 `sh_LoginSceneController` 안의 `TryArchiveOriginalImage(...)`가 호출됩니다.
3. 이 함수는 작업용 원본 파일을 다시 `Application.persistentDataPath/Data/Image/LoginOriginalArchive` 폴더로 복사합니다.
4. 저장한 파일 경로, 파일명, 저장 시각 정보를 `ArchivedImageEntry` 구조체로 관리합니다.
5. 이 목록은 `ArchivedImageQueueData`로 묶어서 `JsonUtility.ToJson(...)`으로 JSON 문자열로 바꿉니다.
6. 변환한 문자열을 `PlayerPrefs`에 저장해서 앱 재실행 후에도 목록 정보를 복원합니다.
7. 로그인 씬이 다시 열리면 `LoadArchivedImageEntries()`가 `PlayerPrefs`를 읽어서 저장 목록을 복구합니다.
8. `RefreshArchivedImagePreviews()`가 각 파일을 다시 스프라이트로 읽어와서 사용자가 배치한 출력 위치에 프리팹으로 생성합니다.

### 왜 파일과 목록을 나눠서 저장했나요
- `PlayerPrefs`는 숫자, 문자열 같은 작은 데이터 저장에는 مناسب하지만, 이미지 파일 자체를 저장하는 용도에는 적합하지 않습니다.
- 그래서 실제 이미지 데이터는 파일로 저장하고, `PlayerPrefs`에는 "어떤 파일이 저장되어 있는지"에 대한 목록 정보만 저장했습니다.
- 이 방식이 용량 관리와 복구 처리 측면에서 더 안정적입니다.

### 최대 6개 큐 관리 방법
- `maxArchivedImageCount`를 6으로 두었습니다.
- 새 원본 이미지가 보관될 때마다 목록 끝에 추가합니다.
- 저장 시각 기준으로 정렬한 뒤, 6개를 초과하면 가장 오래된 항목부터 제거합니다.
- 제거할 때는 목록에서만 빼는 것이 아니라, 실제 기기 저장 파일도 `File.Delete(...)`로 같이 삭제합니다.

### 코드에서 중요한 부분
- `ArchivedImageQueuePlayerPrefsKey`로 보관 목록 메타데이터를 저장합니다.
- `TrimArchivedImageEntriesToLimit()`가 6개 초과 여부를 검사합니다.
- `DeleteArchivedEntryFile(...)`가 오래된 보관 이미지 파일을 실제로 삭제합니다.
- `ResetAllDeviceSavedDataForDeveloper()`를 통해 개발자용 전체 초기화도 가능하게 구성했습니다.

### 사용자가 확인할 점
- `sh_LoginSceneController`의 `Archived Image Preview Prefab`에 원본 이미지 출력 프리팹을 연결해야 합니다.
- `Archived Image Preview Root`에 생성 부모 오브젝트를 연결해야 합니다.
- `Archived Image Preview Points` 배열에 사용자가 직접 배치한 위치 오브젝트를 순서대로 넣어야 합니다.
- 개발자용 초기화 버튼을 만들고 싶다면 `ResetAllDeviceSavedDataForDeveloper()`를 버튼 이벤트에 연결하면 됩니다.

---

## 마무리 정리
- 사진 선택 기능은 사용자의 실제 추억 사진을 앱으로 가져오는 시작점입니다.
- 저장 및 분할 기능은 퍼즐과 AR이 공통으로 사용할 런타임 이미지 데이터를 준비하는 핵심 단계입니다.
- 드래그 기능은 퍼즐 플레이 경험을 직접 만드는 상호작용 기능입니다.
- 기기 원본 저장 기능은 사용자가 이미 사용한 사진을 다시 보여주고 관리하기 위한 보관 기능입니다.

이 4가지 기능이 연결되면서 현재 프로젝트는 `로그인 씬 사진 선택 -> 원본 저장 -> 퍼즐 생성 및 플레이 -> AR 인식 -> 로그인 복귀 후 원본 기록 유지` 흐름으로 동작합니다.
