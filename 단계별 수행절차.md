# 단계별 수행절차

## 1. 문서 목적
- 이 문서는 `기획 기본구조.md`를 기준으로 분리한 `Tasks`와 `CheckList`를 실제 작업 순서로 실행하기 위한 절차서입니다.
- 한 번에 하나의 기능만 구현하고, 유니티 에디터에서 바로 테스트한 뒤 다음 단계로 넘어가는 방식을 기준으로 합니다.

## 2. 진행 원칙
- 항상 `Task` 문서 1개만 진행합니다.
- 구현이 끝나면 바로 대응되는 `CheckList`로 검증합니다.
- 체크리스트를 통과하지 못하면 다음 Task로 넘어가지 않습니다.
- 사용자는 오브젝트 배치와 Inspector 연결을 직접 수행합니다.
- 스크립트 작업은 기본적으로 `ARchive_AI/Assets/Data/Scripts`에 작성합니다.
- 로그인 씬의 UI 텍스트가 한글이라면 한글 폰트와 `TMP Font Asset`을 먼저 준비합니다.
- 안드로이드 실기기 기능은 에디터 테스트와 분리해서 확인합니다.

## 3. 사전 준비

### 3-1. 프로젝트 확인
- 유니티 버전이 `6000.5.4f1`인지 확인합니다.
- 프로젝트가 `ARchive_AI`인지 확인합니다.
- 빌드 타겟이 안드로이드 기준으로 준비되어 있는지 확인합니다.

### 3-2. 폴더 확인
- `ARchive/Tasks`
- `ARchive/CheckList`
- `ARchive_AI/Assets/Data/Scripts`
- `ARchive_AI/Assets/Data/Image`
- `ARchive_AI/Assets/Data/Image/MarkerImage`
- `ARchive_AI/Assets/Data/Image/MarkerImage/RotatingImage`

### 3-3. 씬 확인
- 로그인 씬
- 퍼즐 씬
- AR 씬
- 위 3개 씬이 빌드 세팅에 등록될 예정인지 확인합니다.

### 3-4. 패키지 및 플러그인 확인
- `Window > Package Manager`에서 `Native Gallery` 설치 여부를 확인합니다.
- 설치 방식은 `Add package from git URL...` 기준으로 `https://github.com/yasirkula/UnityNativeGallery.git`를 사용합니다.
- 안드로이드 이미지 선택 기능은 `NativeGallery` 기준으로 테스트합니다.

### 3-5. 한글 폰트 확인
- `ARchive_AI/Assets/Data/Fonts` 폴더에 한글 폰트 파일이 있는지 확인합니다.
- `TextMeshProUGUI`에 한글을 표시할 텍스트가 있으면 `TMP Font Asset`을 생성합니다.
- 로그인 씬 안내 문구가 깨지지 않는지 먼저 확인합니다.

## 4. 단계별 수행 순서

### Step 1. 로그인 씬 이미지 선택 구현
- 작업 문서: `Tasks/01. 로그인씬 이미지선택 구성.md`
- 검증 문서: `CheckList/01. 로그인씬 이미지선택 체크리스트.md`

#### 수행 방법
1. 로그인 씬을 생성하고 `Canvas`, `Button`, `TextMeshProUGUI`를 준비합니다.
2. 빈 오브젝트를 만들고 `sh_LoginSceneController`, `sh_ImagePickerService`를 부착합니다.
3. Inspector에서 `attachFileButton`, `statusText`, `imagePickerService`를 연결합니다.
4. `Native Gallery` 패키지가 설치되어 있는지 확인합니다.
5. 한글 안내 문구를 사용할 경우 `TMP Font Asset`을 만든 뒤 `TextMeshProUGUI`에 연결합니다.
6. 버튼 클릭 시 이미지 선택 창이 열리도록 연결합니다.
7. 선택 성공, 취소, 실패 상태를 각각 처리합니다.

#### 확인 방법
1. 에디터에서 버튼 클릭 시 파일 선택 창이 열리는지 확인합니다.
2. 안드로이드 기기에서 버튼 클릭 시 갤러리 선택 창이 열리는지 확인합니다.
3. 이미지 선택 후 성공 로그 또는 안내 문구가 보이는지 확인합니다.
4. 취소 시 오류 없이 로그인 씬에 머무는지 확인합니다.
5. 권한 거부 시 안내 문구가 정상적으로 보이는지 확인합니다.

#### 통과 후 진행
- 체크리스트 01을 모두 통과하면 Step 2로 넘어갑니다.

### Step 2. 원본 이미지 저장 구현
- 작업 문서: `Tasks/02. 원본이미지 저장 구성.md`
- 검증 문서: `CheckList/02. 원본이미지 저장 체크리스트.md`

#### 수행 방법
1. `sh_ImageStorageService`를 작성합니다.
2. 선택한 이미지를 `Assets/Data/Image` 경로에 저장합니다.
3. 파일명 규칙을 정하고 저장 성공 여부를 반환합니다.
4. 로그인 씬 컨트롤러와 저장 서비스를 연결합니다.
5. 에디터 테스트용 저장 경로와 안드로이드 실기기 저장 경로를 분리해서 점검합니다.

#### 확인 방법
1. 이미지 선택 직후 저장 함수가 호출되는지 확인합니다.
2. 실제 폴더에 원본 이미지 파일이 생성되는지 확인합니다.
3. 저장 실패 시 오류 로그가 출력되는지 확인합니다.
4. 에디터에서는 프로젝트 내부 테스트 경로가 맞는지 확인합니다.
5. 실기기에서는 `persistentDataPath` 등 실제 런타임 저장 위치 전략이 필요한지 점검합니다.

#### 통과 후 진행
- 체크리스트 02를 모두 통과하면 Step 3으로 넘어갑니다.

### Step 3. 1차/2차 이미지 분할 구현
- 작업 문서: `Tasks/03. 1차2차 이미지분할 구성.md`
- 검증 문서: `CheckList/03. 1차2차 이미지분할 체크리스트.md`

#### 수행 방법
1. `sh_ImageSliceService`를 작성합니다.
2. 원본 이미지를 3x3으로 1차 분할합니다.
3. 결과를 `MarkerImage` 폴더에 저장합니다.
4. 1차 조각 9개 중 랜덤 1개를 선택합니다.
5. 선택된 조각을 다시 3x3으로 2차 분할합니다.
6. 결과를 `RotatingImage` 폴더에 저장합니다.

#### 확인 방법
1. `marker_piece_01~09`가 생성되는지 확인합니다.
2. `rotating_piece_01~09`가 생성되는지 확인합니다.
3. 총 18개 이미지가 누락 없이 저장되는지 확인합니다.

#### 통과 후 진행
- 체크리스트 03을 모두 통과하면 Step 4로 넘어갑니다.

### Step 4. 퍼즐 조각 회전 데이터 구현
- 작업 문서: `Tasks/04. 퍼즐조각 회전데이터 구성.md`
- 검증 문서: `CheckList/04. 퍼즐조각 회전데이터 체크리스트.md`

#### 수행 방법
1. `sh_RotationRandomizer`를 작성합니다.
2. 각 2차 조각에 `0`, `90`, `180`, `270` 중 하나의 회전값을 적용합니다.
3. `sh_PuzzlePieceData`를 작성합니다.
4. 조각 번호, 정답 슬롯 번호, 현재 회전값, 이미지 경로를 저장합니다.
5. 랜덤으로 선택된 1차 조각 번호와 2차 조각 목록이 다음 단계에서도 유지되도록 세션 데이터를 정리합니다.

#### 확인 방법
1. 9개 조각 모두 회전값을 가지는지 확인합니다.
2. 각 조각의 정답 슬롯 번호를 조회할 수 있는지 확인합니다.
3. 디버그 로그 또는 Inspector로 데이터가 보이는지 확인합니다.
4. 선택된 1차 조각 번호, 2차 조각 경로, 회전값 목록이 다음 씬 또는 다음 컨트롤러에서 재사용 가능한지 확인합니다.

#### 통과 후 진행
- 체크리스트 04를 모두 통과하면 Step 5로 넘어갑니다.

### Step 5. 퍼즐 UI 생성 구현
- 작업 문서: `Tasks/05. 퍼즐UI 생성 구성.md`
- 검증 문서: `CheckList/05. 퍼즐UI 생성 체크리스트.md`

#### 수행 방법
1. 퍼즐 씬을 준비합니다.
2. 퍼즐 조각 프리팹과 슬롯 오브젝트 9개를 준비합니다.
3. `sh_PuzzleBoardController`를 작성합니다.
4. 이미지 파일을 스프라이트로 변환해 퍼즐 조각 UI에 연결합니다.
5. 9개 조각과 9개 슬롯을 번호 체계로 연결합니다.
6. 퍼즐 씬이 Build Settings에 등록되어 있는지 다시 확인합니다.

#### 확인 방법
1. 퍼즐 씬 진입 시 조각 9개가 보이는지 확인합니다.
2. 조각 이미지가 정상적으로 표시되는지 확인합니다.
3. 슬롯 9개가 구분되고 참조 연결이 맞는지 확인합니다.
4. 퍼즐 씬 이름이 코드에서 사용할 씬 이름과 일치하는지 확인합니다.

#### 통과 후 진행
- 체크리스트 05를 모두 통과하면 Step 6으로 넘어갑니다.

### Step 6. 퍼즐 드래그와 정답 판정 구현
- 작업 문서: `Tasks/06. 퍼즐드래그와 정답판정 구성.md`
- 검증 문서: `CheckList/06. 퍼즐드래그와 정답판정 체크리스트.md`

#### 수행 방법
1. `sh_PuzzleDragHandler`를 작성합니다.
2. 드래그 시작, 이동, 종료 처리를 구현합니다.
3. 슬롯 근처에 두면 스냅되거나 판정되도록 구현합니다.
4. 슬롯 번호와 조각 정답 번호를 비교합니다.
5. 모든 조각이 정답이면 완료 이벤트를 발생시킵니다.

#### 확인 방법
1. 조각을 손가락 또는 마우스로 움직일 수 있는지 확인합니다.
2. 정답 슬롯에서는 정답 처리되는지 확인합니다.
3. 오답 슬롯에서는 완료 처리되지 않는지 확인합니다.
4. 모든 조각 완료 시 이벤트가 1회만 발생하는지 확인합니다.

#### 통과 후 진행
- 체크리스트 06을 모두 통과하면 Step 7로 넘어갑니다.

### Step 7. 퍼즐 완료와 AR 씬 전환 구현
- 작업 문서: `Tasks/07. 퍼즐완료와 AR씬전환 구성.md`
- 검증 문서: `CheckList/07. 퍼즐완료와 AR씬전환 체크리스트.md`

#### 수행 방법
1. `sh_PuzzleResultController`를 작성합니다.
2. 퍼즐 완료 이벤트와 결과 UI를 연결합니다.
3. `기억의 조각을 수집하였습니다` 문구를 표시합니다.
4. `AR 씬으로 이동` 버튼을 활성화합니다.
5. 버튼 클릭 시 AR 씬으로 전환합니다.
6. 퍼즐 씬과 AR 씬이 Build Settings에 모두 등록되어 있는지 다시 확인합니다.

#### 확인 방법
1. 퍼즐 완료 후 메시지가 바로 보이는지 확인합니다.
2. AR 이동 버튼이 완료 후에만 보이는지 확인합니다.
3. 버튼 클릭 시 AR 씬이 열리는지 확인합니다.
4. 씬 이름 오타로 전환 실패가 나지 않는지 확인합니다.

#### 통과 후 진행
- 체크리스트 07을 모두 통과하면 Step 8로 넘어갑니다.

### Step 8. AR 성공 처리와 세션 정리 구현
- 작업 문서: `Tasks/08. AR성공처리와 세션정리 구성.md`
- 검증 문서: `CheckList/08. AR성공처리와 세션정리 체크리스트.md`

#### 수행 방법
1. AR 씬에 `AR Session`, `XR Origin`, `AR Tracked Image Manager`를 준비합니다.
2. `sh_ARSceneController`, `sh_ARTrackingResultHandler`를 작성합니다.
3. 이미지 트래킹 성공 시 결과 UI를 표시합니다.
4. `과거의 추억을 되찾았습니다` 문구를 표시합니다.
5. 로그인 복귀 버튼을 활성화합니다.
6. `sh_SessionCleanupService`를 작성합니다.
7. 복귀 시 원본, 1차, 2차 생성 파일을 삭제합니다.
8. `Reference Image Library`에 어떤 이미지를 넣을지 사전에 정하고 실제 마커 출력물 또는 테스트 화면을 준비합니다.
9. AR 테스트는 안드로이드 실기기 기준으로 진행합니다.

#### 확인 방법
1. 마커 인식 성공 시 성공 메시지가 보이는지 확인합니다.
2. 로그인 복귀 버튼이 정상 작동하는지 확인합니다.
3. 로그인 씬 복귀 후 생성 파일이 삭제되는지 확인합니다.
4. `AR Tracked Image Manager`의 라이브러리 연결이 맞는지 확인합니다.
5. 준비한 실제 마커가 카메라에서 안정적으로 인식되는지 확인합니다.

#### 통과 후 진행
- 체크리스트 08을 모두 통과하면 1차 제작 흐름이 완료됩니다.

## 5. 매 단계 공통 작업 방식
1. 해당 Task 문서를 읽습니다.
2. 필요한 스크립트를 작성합니다.
3. 유니티에서 오브젝트와 Inspector 참조를 연결합니다.
4. UI에 한글이 있으면 폰트와 `TMP Font Asset` 연결 상태를 먼저 확인합니다.
5. 에디터에서 1차 테스트를 진행합니다.
6. 안드로이드 전용 기능이면 실기기에서 다시 테스트합니다.
7. Console 에러를 먼저 확인한 뒤 대응 `CheckList`를 한 줄씩 점검합니다.
8. 실패 항목이 있으면 바로 수정합니다.
9. 모든 항목 통과 후 다음 단계로 넘어갑니다.

## 6. 권장 테스트 순서
1. 씬과 UI 연결 상태 확인
2. 한글 폰트와 `TMP Font Asset` 표시 확인
3. 에디터에서 기본 흐름 테스트
4. 안드로이드 기기에서 이미지 선택 테스트
5. 이미지 저장 경로와 생성 파일 직접 확인
6. 퍼즐 씬 드래그 조작 테스트
7. 씬 전환 테스트
8. AR 마커 인식 테스트

## 7. Step 1 추가 확인 사항
- 현재 이미지 선택 기능은 `sh_ImagePickerService`가 담당합니다.
- 에디터에서는 시스템 파일 선택 창으로 테스트합니다.
- 안드로이드에서는 `NativeGallery` 패키지가 설치되어 있어야 합니다.
- 실기기에서 처음 실행하면 갤러리 권한 요청이 나타날 수 있습니다.
- `TextMeshProUGUI`에 한글이 깨지면 폰트 파일만 넣은 상태일 수 있으므로 `TMP Font Asset`을 다시 확인합니다.

## 8. 최종 완료 기준
- 로그인 씬에서 사용자가 이미지를 선택할 수 있습니다.
- 선택한 이미지를 기준으로 퍼즐 조각이 생성됩니다.
- 퍼즐 조각을 맞춰 완료할 수 있습니다.
- 완료 후 AR 씬으로 이동할 수 있습니다.
- AR 마커 인식 성공 후 로그인 씬으로 복귀할 수 있습니다.
- 복귀 시 이번 세션의 생성 파일이 정리됩니다.
