# 기획 기본구조

## 1. 문서 목적
이 문서는 사용자가 제시한 구현 방향을 기준으로, `ARchive_AI` 프로젝트에서 퍼즐 기반 기억 회복형 AR 게임을 제작하기 위한 기본 구조를 정리한 문서입니다.

## 2. 게임 개요
- 사용자는 로그인 씬에서 시작합니다.
- 퍼즐 플레이에 사용할 사진을 직접 첨부합니다.
- 첨부한 사진을 기반으로 퍼즐 조각 이미지를 생성합니다.
- 사용자는 조각을 드래그하여 원본 사진 형태로 다시 맞춥니다.
- 퍼즐을 완성하면 `기억의 조각을 수집하였습니다` 문구와 함께 AR 씬으로 이동하는 버튼이 생성됩니다.
- AR 씬에서는 퍼즐로 완성한 이미지를 기반으로 이미지 트래킹을 수행합니다.
- 현실의 마커 이미지를 카메라가 인식하면 `과거의 추억을 되찾았습니다` 문구와 함께 로그인 씬으로 돌아가는 버튼이 생성됩니다.
- 로그인 씬으로 돌아갈 때 이번 플레이에서 생성한 이미지 파일들을 정리 삭제합니다.

## 3. 핵심 경험 목표
- 사용자가 직접 고른 사진으로 퍼즐을 풀며 기억 회복 감성을 느끼게 합니다.
- 퍼즐 완료 후 AR 인식으로 이어지는 흐름을 통해 성취감을 강화합니다.
- 노년층 대상이므로 복잡한 조작을 줄이고, 화면 전환과 메시지를 명확하게 제공합니다.

## 4. 전체 진행 흐름
1. 로그인 씬 진입
2. 파일 첨부 버튼 클릭
3. 안드로이드 이미지 선택 창 호출
4. 사용자 사진 선택
5. 원본 이미지 저장
6. 원본 이미지 9등분 생성
7. 9등분 결과를 `MarkerImage` 폴더에 저장
8. 9개 중 랜덤 1개 선택
9. 선택된 1개 조각을 다시 9등분 생성
10. 생성 결과를 `RotatingImage` 폴더에 저장
11. `RotatingImage` 내부 각 조각에 랜덤 회전값 적용
12. 생성된 이미지를 퍼즐 UI용 Source Image로 사용 가능하도록 준비
13. 퍼즐 씬에서 드래그 이동을 통해 정답 배치 유도
14. 퍼즐 정답 판정
15. 완료 메시지와 AR 씬 이동 버튼 노출
16. AR 씬 진입
17. 완성된 이미지를 마커 기준으로 이미지 트래킹 수행
18. 마커 인식 성공 시 완료 메시지와 로그인 씬 복귀 버튼 노출
19. 로그인 씬 복귀 시 생성 이미지 정리 삭제

## 5. 기준 폴더 구조

### 문서 및 비유니티 자료
- `ARchive\기획 기본구조.md`
- `ARchive\개선안 제안서.md`

### 유니티 내부 이미지 관련 경로
- 원본 이미지 저장 경로: `ARchive_AI\Assets\Data\Image`
- 1차 분할 이미지 저장 경로: `ARchive_AI\Assets\Data\Image\MarkerImage`
- 2차 분할 및 회전용 이미지 저장 경로: `ARchive_AI\Assets\Data\Image\MarkerImage\RotatingImage`

### 유니티 내부 스크립트 경로
- 새로 작성하는 스크립트 기본 경로: `ARchive_AI\Assets\Data\Scripts`

## 6. 씬 구성 기본안

### 6-1. 로그인 씬
- 시작 화면 역할
- 파일 첨부 버튼 배치
- 게임 시작 또는 퍼즐 씬 진입 버튼 배치
- 최근 플레이 안내 문구 또는 설명 문구 배치 가능

### 6-2. 퍼즐 씬
- 원본 이미지를 기준으로 생성된 퍼즐 조각 표시
- 조각 드래그 이동 기능 제공
- 퍼즐 정답 판정 기능 포함
- 완료 시 메시지와 AR 씬 이동 버튼 표시

### 6-3. AR 씬
- 카메라 기반 이미지 트래킹 수행
- 마커 인식 성공 시 메시지 출력
- 로그인 씬으로 돌아가는 버튼 표시

## 7. 사용자 제안 방식 기준 상세 기능 구조

### 7-1. 파일 첨부 기능
- 사용자가 파일 첨부 버튼을 누르면 안드로이드 기기의 이미지 선택 창을 엽니다.
- 선택 가능한 파일 형식은 우선 `jpg`, `jpeg`, `png`를 기준으로 잡습니다.
- 선택 완료 후 게임에서 사용할 임시 작업용 이미지 경로로 파일을 저장합니다.

### 7-2. 원본 이미지 저장 기능
- 선택한 이미지는 `ARchive_AI\Assets\Data\Image` 경로에 저장합니다.
- 저장 시 이전 플레이의 잔여 파일과 구분할 수 있도록 규칙적인 파일명 체계를 사용합니다.
- 예시 파일명:
- `user_source.jpg`

### 7-3. 1차 9등분 생성 기능
- 원본 이미지를 동일한 비율 기준으로 3 x 3 총 9개로 분할합니다.
- 각 조각은 원본의 좌상단부터 우하단까지 순서대로 번호를 가집니다.
- 생성된 결과는 `MarkerImage` 폴더에 저장합니다.
- 예시 파일명:
- `marker_piece_01.png`
- `marker_piece_02.png`
- `marker_piece_03.png`
- `marker_piece_04.png`
- `marker_piece_05.png`
- `marker_piece_06.png`
- `marker_piece_07.png`
- `marker_piece_08.png`
- `marker_piece_09.png`

### 7-4. 랜덤 조각 재분할 기능
- 1차 분할로 생성된 9개 조각 중 랜덤으로 1개를 선택합니다.
- 선택한 1개 조각을 다시 3 x 3으로 동일 비율 분할합니다.
- 생성된 결과는 `RotatingImage` 폴더에 저장합니다.
- 번호 체계는 다시 좌상단부터 우하단까지 `1~9` 순서로 부여합니다.
- 예시 파일명:
- `rotating_piece_01.png`
- `rotating_piece_02.png`
- `rotating_piece_03.png`
- `rotating_piece_04.png`
- `rotating_piece_05.png`
- `rotating_piece_06.png`
- `rotating_piece_07.png`
- `rotating_piece_08.png`
- `rotating_piece_09.png`

### 7-5. 랜덤 회전 기능
- `RotatingImage` 폴더에 생성된 9개 이미지 각각에 대해 랜덤 회전값을 적용합니다.
- 허용 회전값은 `0`, `90`, `180`, `270`입니다.
- 이 회전값은 퍼즐 난이도 요소로 사용됩니다.

### 7-6. UI Source Image 사용 준비
- 퍼즐에 쓰일 이미지는 유니티 UI `Image` 컴포넌트의 `Source Image`로 사용 가능해야 합니다.
- 따라서 코드상에서 불러온 텍스처를 스프라이트로 변환하고 UI에 연결 가능한 상태로 처리합니다.
- 필요 시 텍스처 읽기 가능 여부, 스프라이트 생성 기준, 픽셀 보존 여부를 함께 검토합니다.

### 7-7. 퍼즐 판정 기능
- 사용자가 각 조각을 드래그하여 배치합니다.
- 각 조각이 자신에게 맞는 슬롯에 들어가면 정답 상태로 간주합니다.
- 모든 조각이 정답 상태가 되면 퍼즐 완료로 판정합니다.

### 7-8. 완료 메시지 및 AR 전환 기능
- 퍼즐 완료 즉시 `기억의 조각을 수집하였습니다` 문구를 표시합니다.
- 동시에 `AR 씬으로 이동` 버튼을 생성 또는 활성화합니다.
- 버튼 클릭 시 AR 씬으로 전환합니다.

### 7-9. AR 이미지 트래킹 기능
- 퍼즐을 통해 완전해진 사진을 이미지 트래킹 기준 이미지로 사용합니다.
- 현실에 미리 배치한 마커 이미지를 카메라가 인식하면 성공 처리합니다.
- 인식 성공 시 `과거의 추억을 되찾았습니다` 문구를 보여줍니다.
- 동시에 로그인 씬으로 돌아가는 버튼을 생성 또는 활성화합니다.

### 7-10. 생성 파일 정리 기능
- 사용자가 로그인 씬 복귀 버튼을 누르면 로그인 씬으로 이동합니다.
- 씬 이동과 함께 이번 세션에서 생성한 이미지 파일을 정리 삭제합니다.
- 삭제 대상:
- `ARchive_AI\Assets\Data\Image` 내부 원본 이미지
- `ARchive_AI\Assets\Data\Image\MarkerImage` 내부 1차 분할 이미지
- `ARchive_AI\Assets\Data\Image\MarkerImage\RotatingImage` 내부 2차 분할 이미지

## 8. 추천 데이터 관리 구조
- 원본 이미지 경로
- 1차 분할 이미지 목록
- 랜덤 선택된 1차 조각 정보
- 2차 분할 이미지 목록
- 각 2차 조각의 현재 회전값
- 퍼즐 정답 슬롯 번호
- AR 트래킹에 사용할 기준 이미지 정보
- 현재 게임 진행 상태

## 9. 추천 스크립트 역할 구조
- `sh_LoginSceneController`
  로그인 씬의 버튼 이벤트와 다음 씬 이동 제어
- `sh_ImagePickerService`
  안드로이드 이미지 선택 기능 담당
- `sh_ImageStorageService`
  원본/분할/회전 이미지 저장과 삭제 담당
- `sh_ImageSliceService`
  1차 9등분, 2차 9등분 처리 담당
- `sh_RotationRandomizer`
  회전값 랜덤 적용 담당
- `sh_PuzzlePieceData`
  퍼즐 조각 데이터 보관
- `sh_PuzzleBoardController`
  퍼즐 조각 생성, 슬롯 매칭, 완료 판정 담당
- `sh_PuzzleDragHandler`
  퍼즐 조각 드래그 동작 담당
- `sh_PuzzleResultController`
  완료 메시지와 AR 씬 이동 버튼 처리
- `sh_ARSceneController`
  AR 씬 흐름 제어
- `sh_ARTrackingResultHandler`
  이미지 트래킹 성공 처리와 복귀 버튼 제어
- `sh_SessionCleanupService`
  로그인 씬 복귀 시 생성 파일 삭제 담당

## 10. 유니티 오브젝트 및 설정 안내 포인트
- 로그인 씬의 파일 첨부 버튼에는 `Button` 컴포넌트가 필요합니다.
- 퍼즐 조각 UI에는 `Image`, 드래그 처리용 스크립트, 필요 시 `CanvasGroup`이 필요합니다.
- 퍼즐 슬롯 판정을 위해 슬롯 오브젝트별 식별 번호 관리가 필요합니다.
- AR 씬에는 `AR Session`과 현재 프로젝트 버전에 맞는 `XR Origin` 계열 오브젝트가 필요합니다.
- 이미지 트래킹을 위해 `AR Tracked Image Manager`와 Reference Image Library 설정이 필요합니다.
- 씬 전환을 위해 빌드 세팅에 로그인 씬, 퍼즐 씬, AR 씬이 모두 등록되어야 합니다.

## 11. 주의사항
- 안드로이드 기기에서 선택한 이미지를 `Assets` 내부에 직접 저장하는 구조는 에디터와 실제 빌드 환경에서 차이가 있을 수 있습니다.
- 런타임 중 생성한 이미지를 바로 AR Reference Image Library에 반영하는 방식은 구현 난도가 높을 수 있습니다.
- 노년층 대상이므로 퍼즐 조각 수, 드래그 감도, 버튼 크기, 글자 크기를 충분히 크게 설계하는 것이 중요합니다.

## 12. 1차 제작 우선순위
1. 로그인 씬에서 이미지 첨부 기능 구현
2. 원본 이미지 저장 기능 구현
3. 1차/2차 이미지 분할 기능 구현
4. UI용 스프라이트 변환 기능 구현
5. 퍼즐 조각 드래그 및 정답 판정 구현
6. 퍼즐 완료 메시지와 AR 씬 이동 기능 구현
7. AR 이미지 트래킹 성공 처리 구현
8. 로그인 씬 복귀 및 생성 파일 정리 구현

## 13. 완료 기준
- 사용자가 이미지를 선택할 수 있어야 합니다.
- 선택한 이미지를 기준으로 퍼즐 플레이가 가능해야 합니다.
- 퍼즐 완료 후 AR 씬으로 자연스럽게 이어져야 합니다.
- 마커 인식 성공 후 로그인 씬으로 복귀할 수 있어야 합니다.
- 복귀 시 생성 이미지가 정리 삭제되어야 합니다.
