# AGENTS.md

## 목적
이 문서는 `ARchive_AI` 유니티 프로젝트에서 작업하는 에이전트가 일관된 방식으로 지원할 수 있도록 공통 작업 기준을 정의합니다.

## 프로젝트 개요
- 본 프로젝트는 안드로이드 기반 유니티 앱입니다.
- 현재 작업 기준 유니티 버전은 `6000.5.4f1`입니다.
- 대상 사용자는 노년층이며, 치매 예방 및 기억력 향상을 위한 퍼즐 게임을 제작합니다.
- 게임 흐름은 `2D 로그인/진입 -> 퍼즐 플레이 -> 퍼즐 완료 후 AR 씬 전환` 구조를 기준으로 합니다.
- 핵심 기능은 `퍼즐 게임 요소`와 `AR 마커 이미지 인식 기능`입니다.
- 기준 작업 해상도는 세로형 `1920 x 1080`입니다.

## 에이전트의 역할
- 사용자가 요청한 기능에 대해 유니티 프로젝트 내부 C# 스크립트를 작성합니다.
- 프로젝트 진행 중 발생한 오류 로그 또는 예외 내용을 전달받으면 원인 분석과 수정안을 제공합니다.
- 이미 작성한 스크립트나 프로젝트 구성에서 개선 요청이 있으면 해당 내용을 반영합니다.
- 사용자는 씬 오브젝트를 직접 배치하고 위치를 조정하므로, 에이전트는 오브젝트 배치 자체보다 필요한 컴포넌트 구성과 Inspector 설정값을 구체적으로 안내합니다.

## 스크립트 작성 위치
- 새로 작성하는 런타임 스크립트는 기본적으로 `Assets/Data/Scripts` 폴더에 생성합니다.
- 필요 시 하위 폴더를 만들 수 있지만, 특별한 이유가 없다면 우선 `Assets/Data/Scripts`에 작성합니다.
- 사용자가 별도 위치를 명시한 경우 그 지시를 우선합니다.

## 파일 생성 위치 원칙
- 유니티 내부에서 사용될 스크립트, 프리팹, 씬, 설정 파일 등은 `ARchive_AI` 프로젝트 내부에 생성합니다.
- 유니티 내부에서 직접 사용되지 않는 문서, 기획 메모, 작업 기록, 참고 자료 등은 사용자가 별도 경로를 지정하지 않는 한 `ARchive` 폴더에 생성합니다.
- 현재 이 `AGENTS.md` 역시 프로젝트 작업 지침 문서이므로 `ARchive` 루트에서 관리합니다.

## 코드 작성 규칙
- Unity C#의 일반적인 코딩 관례를 따릅니다.
- 새로 만드는 클래스 이름에는 반드시 `sh_` 접두사를 사용합니다.
- 클래스명, 파일명, MonoBehaviour 타입명은 서로 일치시킵니다.
- 직렬화가 필요한 필드는 `private` + `[SerializeField]` 사용을 우선 검토합니다.
- Inspector에서 조정 가능한 값은 하드코딩보다 직렬화 필드로 노출하는 방식을 우선합니다.
- 사용자 초보자를 고려해, 복잡한 처리에는 짧고 명확한 주석을 추가할 수 있습니다.

## 유니티 작업 방식
- 모든 설명과 스크립트 제안은 기본적으로 Unity `6000.5.4f1` 환경을 기준으로 작성합니다.
- 씬에 배치할 오브젝트의 생성, 배치, 위치 수정은 사용자가 직접 수행합니다.
- 에이전트는 스크립트를 제공할 때 다음 내용을 함께 안내해야 합니다.
- 어떤 게임 오브젝트에 스크립트를 붙여야 하는지
- 추가로 필요한 컴포넌트가 무엇인지
- Inspector에서 연결해야 하는 참조가 무엇인지
- 태그, 레이어, Collider, Rigidbody, Canvas 설정 등 사용자가 직접 맞춰야 하는 항목이 있는지
- 씬 전환, AR Foundation, UI 버튼 연결 등에서 사전 설정이 필요한지

## AR Foundation 관련 지침
- AR 기능을 다룰 때는 마커 이미지 인식에 필요한 구성 요소를 빠뜨리지 않고 안내합니다.
- Unity `6000.5.4f1`과 현재 프로젝트에 설치된 패키지 구성을 기준으로 설명합니다.
- 예를 들어 `AR Session`, 현재 버전에 맞는 `XR Origin` 계열 구성, `AR Tracked Image Manager`, Reference Image Library 연결 여부 등을 확인 대상으로 포함합니다.
- 예전 문서나 구버전 예시의 오브젝트 명칭과 차이가 있을 수 있으므로, 답변 시 현재 프로젝트에서 실제로 쓰는 명칭을 우선합니다.

## 응답 방식
- 항상 존댓말로 설명합니다.
- 사용자를 완전 초보자로 가정하고, 바로 실행할 수 있도록 단계별로 설명합니다.
- 코드를 제공할 때는 단순히 스크립트만 전달하지 말고, 적용 방법과 확인 방법도 함께 설명합니다.
- 오류 수정 시에는 `원인`, `수정 내용`, `사용자가 확인할 점`을 분리해서 설명합니다.

## 우선순위
- 사용자의 직접 지시가 이 문서보다 우선합니다.
- 프로젝트 내부 기존 구조와 충돌하지 않는 방향으로 최소 변경을 우선합니다.
- 기능 구현뿐 아니라 사용자가 유니티 에디터에서 무엇을 해야 하는지도 반드시 알려줍니다.
