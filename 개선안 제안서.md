# 개선안 제안서

## 1. 문서 목적
이 문서는 사용자가 제시한 기본 구현안을 유지하되, 실제 유니티 `6000.5.4f1` 및 안드로이드 환경에서 더 안정적이고 확장성 있게 제작할 수 있는 개선 방향을 제안하기 위한 문서입니다.

## 2. 현재 구상안의 장점
- 사용자가 직접 고른 사진으로 몰입감을 높일 수 있습니다.
- 퍼즐 완료와 AR 인식이 자연스럽게 연결되어 주제 일관성이 좋습니다.
- 치매 예방 테마와 `기억 회복` 메시지가 잘 맞습니다.

## 3. 현재 구상안에서 예상되는 구현 리스크
- 런타임 중 `Assets` 폴더에 파일을 저장하는 방식은 실제 안드로이드 빌드에서 다루기 까다롭습니다.
- 런타임 생성 이미지를 즉시 AR 이미지 트래킹 라이브러리에 반영하는 부분은 AR Foundation 기본 흐름만으로는 제한이 있을 수 있습니다.
- 원본 이미지를 9등분한 뒤 다시 1개 조각을 9등분하는 방식은 시각적으로 지나치게 복잡해져 노년층 난이도에 부담이 될 수 있습니다.
- 회전 퍼즐까지 동시에 넣으면 드래그와 방향 인지를 모두 요구하므로 조작 피로가 커질 수 있습니다.
- 생성 파일이 많아지면 관리, 삭제, 재시작 시 안정성 점검 포인트가 늘어납니다.

## 4. 개선 구현안 제안

### 4-1. 저장 경로 개선
- 유니티 런타임에서 실제 저장은 `Application.persistentDataPath`를 사용합니다.
- `Assets/Data/Image` 계열 폴더는 개발 중 샘플 리소스 또는 초기 테스트 용도로만 사용합니다.
- 세션 중 생성되는 원본/분할 이미지는 영구 데이터 경로에 저장하고, 게임 내부에서는 해당 경로를 참조해 로드합니다.

### 4-2. AR 트래킹 이미지 운영 방식 개선
- 사용자 사진 전체를 즉시 동적 AR 마커로 만드는 대신, 2가지 방식 중 하나를 권장합니다.
- 방식 A: 사용자가 제공한 사진을 기준으로 미리 정의된 프레임 또는 보정 레이아웃을 합성해 마커 품질을 높인 후 사용
- 방식 B: 사용자 사진은 퍼즐 완료 연출용으로 사용하고, 실제 AR 트래킹은 사전에 등록한 고품질 마커 세트 중 하나와 연결
- 초반 프로토타입은 방식 B가 더 안전합니다.

### 4-3. 퍼즐 난이도 구조 개선
- 1차 버전에서는 `3 x 3 일반 위치 맞추기 퍼즐`만 우선 구현합니다.
- 회전 퍼즐은 옵션 모드 또는 2차 업데이트로 분리합니다.
- 노년층 대상 기준으로는 `조각 이동만`, `힌트 표시`, `자동 스냅`, `오답 위치 완화 판정`이 더 적합합니다.

### 4-4. 이미지 가공 파이프라인 개선
- 원본 이미지 업로드 직후 다음 순서로 통일합니다.
1. 원본 비율 확인
2. 퍼즐용 안전 영역 크롭 또는 패딩
3. 퍼즐 조각 생성
4. UI 스프라이트 변환
5. 마커용 대표 이미지 별도 생성
- 이렇게 분리하면 퍼즐용 이미지와 AR용 이미지를 다른 품질 기준으로 관리할 수 있습니다.

### 4-5. 데이터 구조 개선
- 현재 세션 정보를 하나의 세션 데이터 객체로 관리합니다.
- 포함 항목 예시:
- 세션 ID
- 원본 이미지 경로
- 퍼즐 조각 경로 목록
- 정답 인덱스 목록
- 현재 배치 상태
- 사용 시간
- AR 성공 여부
- 정리 삭제 대상 경로 목록

### 4-6. 씬 구조 개선
- 로그인 씬
  사용자 진입, 이미지 선택, 설명 제공
- 퍼즐 씬
  퍼즐 플레이와 완료 처리
- AR 씬
  인식 성공 연출과 회상 메시지 표시
- 결과 씬 또는 팝업
  플레이 요약, 다시 시작, 로그인 복귀

### 4-7. 접근성 개선
- 버튼 터치 영역 확대
- 텍스트 크기 확대
- 고대비 색상 사용
- 진행 안내 음성 또는 효과음 보조 고려
- 현재 해야 할 행동을 한 줄로 고정 안내

## 5. 권장 기술 구조

### 5-1. 서비스 분리 구조
- `sh_ImageImportService`
  기기 이미지 가져오기 담당
- `sh_RuntimeImageRepository`
  런타임 저장 및 불러오기 담당
- `sh_PuzzleGenerationService`
  퍼즐 데이터 생성 담당
- `sh_PuzzlePlayController`
  퍼즐 진행 담당
- `sh_ARMarkerFlowController`
  AR 전환 및 인식 흐름 담당
- `sh_GameSessionController`
  세션 데이터, 진행 상태, 종료 정리 담당

### 5-2. 추천 개발 순서
1. 샘플 이미지 기반 퍼즐 기능 완성
2. 사용자 이미지 가져오기 연결
3. 런타임 저장 구조 안정화
4. 퍼즐 완료 플로우 연결
5. AR 씬 인식 성공 흐름 구현
6. 접근성 보정과 난이도 조정

## 6. 기본안 대비 실무형 개선 포인트 요약
- `Assets` 직접 저장보다 `persistentDataPath` 사용이 안전합니다.
- AR 마커는 사용자 사진 그대로보다 인식 최적화된 대표 이미지 운영이 안정적입니다.
- 2중 9등분 + 랜덤 회전은 초반 난이도가 너무 높을 수 있습니다.
- 퍼즐용 이미지와 AR용 이미지를 분리 관리하면 품질 제어가 쉬워집니다.
- 세션 데이터 객체를 두면 삭제, 복귀, 재시작 처리가 깔끔해집니다.

## 7. 색다른 게임안 제시

### 제안 1. 추억 순서 복원 게임
- 여러 장의 가족 사진이나 장소 사진을 시간 순서대로 배치하게 하는 방식입니다.
- AR 인식 성공 시 해당 사진과 연결된 추억 메시지를 띄워줄 수 있습니다.
- 기억 회상과 순서 기억 훈련에 적합합니다.

### 제안 2. 사물 매칭 기억 게임
- 사용자가 익숙한 물건 사진을 보고 같은 계열의 실제 마커를 찾아 카메라로 비추는 방식입니다.
- 예를 들어 컵, 안경, 시계, 책 같은 친숙한 대상 중심으로 구성할 수 있습니다.
- 시각 인지와 연상 기억을 함께 자극할 수 있습니다.

### 제안 3. 공간 회상 산책 게임
- 집 안 또는 복지관 공간에 마커를 배치하고, 안내 문구를 따라 특정 위치의 추억 사진을 찾는 방식입니다.
- AR 인식 성공 시 다음 목적지를 알려주는 구조로 확장할 수 있습니다.
- 이동과 탐색이 결합되어 활동성이 높아집니다.

### 제안 4. 가족 목소리 회상 게임
- 퍼즐을 완성하거나 AR 마커를 인식하면 가족이 녹음한 짧은 음성이 재생되는 방식입니다.
- 시각 정보뿐 아니라 청각 자극을 함께 활용할 수 있습니다.
- 정서적 몰입이 강하고 기억 자극 효과가 큽니다.

### 제안 5. 계절 추억 수집 게임
- 봄, 여름, 가을, 겨울 테마 사진을 퍼즐로 복원하고, AR 인식 시 계절별 추억 오브젝트가 나타나는 방식입니다.
- 계절과 경험을 연결하는 회상 훈련에 적합합니다.
- 반복 플레이 시 콘텐츠 묶음 구성이 쉽습니다.

### 제안 6. 하루 일과 회상 게임
- 아침, 점심, 저녁, 산책, 취침 전 같은 하루 흐름을 상징하는 사진을 퍼즐 또는 카드로 맞추게 합니다.
- AR 단계에서는 해당 일과와 연결된 오브젝트나 문구를 보여줄 수 있습니다.
- 생활 루틴 기억 강화에 활용하기 좋습니다.

## 8. 추천 최종 방향
- 1차 프로토타입은 `사용자 이미지 선택 -> 3 x 3 퍼즐 완성 -> AR 성공 연출 -> 복귀`만 먼저 구현하는 것이 좋습니다.
- 회전 퍼즐, 2차 9등분, 동적 마커 고도화는 2차 단계로 넘기는 편이 안전합니다.
- 치매 예방 테마를 살리려면 난이도보다 `회상`, `성취`, `반복 가능한 안정적 흐름`을 우선하는 것이 좋습니다.
